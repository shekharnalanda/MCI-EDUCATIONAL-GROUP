@extends('admin.layouts.app')
@section('title','Follow-ups')
@section('content')
<h2 class="fw-bold mb-4">Follow-up Manager</h2><div class="card p-3 mb-4"><form class="row g-2"><div class="col-md-4"><select class="form-select" name="status"><option value="">All Status</option>@foreach(['pending','completed','cancelled'] as $v)<option value="{{ $v }}" @selected(request('status')===$v)>{{ ucfirst($v) }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form></div>
@forelse($items as $item)<div class="card p-3 mb-3"><div class="row g-3 align-items-start"><div class="col-md-3"><strong>Enquiry #{{ $item->enquiry_id }}</strong><br><small>{{ $item->enquiry?->institution?->name }} · {{ $item->enquiry?->name }}</small></div><div class="col-md-2"><span class="badge text-bg-light">{{ $item->status }}</span><br><small>{{ optional($item->scheduled_at)->format('d M Y H:i') }}</small></div><div class="col-md-3"><small>{{ $item->note }}</small></div><div class="col-md-2">@if($item->enquiry)<a class="btn btn-outline-primary w-100" href="{{ route('admin.enquiries.show',$item->enquiry) }}">Open Enquiry</a>@endif</div><div class="col-md-2">@if($item->status==='pending')<form method="POST" action="{{ route('admin.follow-ups.complete',$item) }}">@csrf @method('PATCH')<input class="form-control form-control-sm mb-1" name="outcome" placeholder="Outcome"><button class="btn btn-success btn-sm w-100">Complete</button></form>@endif</div></div></div>@empty<div class="alert alert-info">No follow-ups found.</div>@endforelse
{{ $items->links() }}
@endsection
