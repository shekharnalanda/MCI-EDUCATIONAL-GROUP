@extends('admin.layouts.app')
@section('title','Admission Detail')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="fw-bold mb-1">Admission #{{ $admission->id }}</h2><p class="text-muted mb-0">{{ $admission->institution?->name ?: 'Unassigned business' }}</p></div><a href="{{ route('admin.admissions.index') }}" class="btn btn-outline-secondary">Back</a></div>
<div class="row g-4"><div class="col-lg-7"><div class="card p-4"><h5>{{ $admission->applicant_name }}</h5><p class="mb-1">{{ $admission->phone }} · {{ $admission->email }}</p><p class="mb-1"><strong>Course/Program:</strong> {{ $admission->course_program ?: '-' }}</p><p class="mb-1"><strong>Application Ref:</strong> {{ $admission->application_reference ?: '-' }}</p><p class="mb-1"><strong>Source:</strong> {{ $admission->source_site ?: '-' }}</p><p class="mb-0"><strong>Submitted:</strong> {{ optional($admission->submitted_at)->format('d M Y H:i') }}</p></div></div><div class="col-lg-5"><div class="card p-4"><h5>Update</h5><form method="POST" action="{{ route('admin.admissions.update',$admission) }}">@csrf @method('PATCH')<label class="form-label">Status</label><select name="status" class="form-select mb-3">@foreach(['new','under_review','approved','admitted','rejected','cancelled','closed'] as $s)<option value="{{ $s }}" @selected($admission->status===$s)>{{ ucwords(str_replace('_',' ',$s)) }}</option>@endforeach</select><label class="form-label">Payment Status</label><select name="payment_status" class="form-select mb-3">@foreach(['unknown','pending','partial','paid','failed','refunded'] as $s)<option value="{{ $s }}" @selected($admission->payment_status===$s)>{{ ucfirst($s) }}</option>@endforeach</select><label class="form-label">Course/Program</label><input name="course_program" class="form-control mb-3" value="{{ $admission->course_program }}"><button class="btn btn-primary w-100">Save</button></form></div></div></div>
@endsection
