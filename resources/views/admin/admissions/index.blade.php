@extends('admin.layouts.app')
@section('title','Admissions')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="fw-bold mb-1">Central Admissions</h2><p class="text-muted mb-0">Applications received across MCI Educational Group.</p></div>@if($canDelete)<a class="btn btn-outline-secondary" href="{{ route('admin.admissions.index',['trash'=>request('trash')?0:1]) }}">{{ request('trash')?'Back to Active':'Trash' }}</a>@endif</div>
<div class="card p-3 mb-4"><form class="row g-2" method="GET">@if(request('trash'))<input type="hidden" name="trash" value="1">@endif<div class="col-md-3"><select class="form-select" name="institution_id"><option value="">All Businesses</option>@foreach($institutions as $i)<option value="{{ $i->id }}" @selected(request('institution_id')==$i->id)>{{ $i->name }}</option>@endforeach</select></div><div class="col-md-2"><select class="form-select" name="status"><option value="">All Status</option>@foreach(['new','under_review','approved','admitted','rejected','cancelled','closed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucwords(str_replace('_',' ',$s)) }}</option>@endforeach</select></div><div class="col-md-2"><select class="form-select" name="payment_status"><option value="">Payment</option>@foreach(['unknown','pending','partial','paid','failed','refunded'] as $s)<option value="{{ $s }}" @selected(request('payment_status')===$s)>{{ ucfirst($s) }}</option>@endforeach</select></div><div class="col-md-3"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Name, mobile, email, course, ref"></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form></div>
@forelse($items as $item)<div class="card p-3 mb-3"><div class="row align-items-center g-2"><div class="col-md-3"><strong>{{ $item->applicant_name }}</strong><br><small>{{ $item->phone }} {{ $item->email }}</small></div><div class="col-md-3"><div>{{ $item->institution?->name ?: 'Unassigned' }}</div><small class="text-muted">{{ $item->course_program ?: '-' }}</small></div><div class="col-md-2"><span class="badge text-bg-light">{{ $item->status }}</span><br><small>Payment: {{ $item->payment_status }}</small></div><div class="col-md-2"><small>{{ optional($item->submitted_at)->format('d M Y H:i') }}</small></div><div class="col-md-2">@if($item->trashed())<form method="POST" action="{{ route('admin.admissions.restore',$item->id) }}">@csrf<button class="btn btn-outline-success w-100">Restore</button></form>@else<a class="btn btn-outline-primary w-100" href="{{ route('admin.admissions.show',$item) }}">Open</a>@endif</div></div></div>@empty<div class="alert alert-info">No admissions found.</div>@endforelse
{{ $items->links() }}
@endsection
